# 🖥️ TUI 게임 프로젝트 아이디어 모음

> 터미널 UI 기반 GitHub 배포 가능 프로젝트 아이디어  
> 생성일: 2026-06-24 | 최종 업데이트: 2026-06-24 (라운드 2) | 작성: ICBM2

---

## 📌 배경

이 문서는 터미널(CLI/TUI) 환경에서 실행되며, 화면만 봐서는 일(work)인지 게임(play)인지 구분이 어려운 "업무 위장형" 게임 아이디어를 모았습니다. Python의 `textual`, `rich`, Go의 `bubbletea`, Rust의 `ratatui` 같은 TUI 프레임워크로 구현하며, 기본적으로 **무료 오픈소스**로 GitHub 릴리즈 배포를 목표로 합니다.

핵심 컨셉은 "**업무 중 옆에서 봐도 의심받지 않는 화면**"입니다. htop, vim, gdb, kubectl 같은 익숙한 도구의 외형을 차용해 안에서 경영 시뮬레이션, RPG, 추리 게임, RTS 등을 실행합니다.

**라운드 1 (PART 1\~5)**: 시스템 모니터 / 에디터 / 대시보드 / 디버거 / 클러스터 위장  
**라운드 2 (PART 6\~12)**: 로그 스트림 / 스프레드시트 / 보안 로그 / 스케줄러 / 네트워크 / 칸반 / VCS 위장

### 참고: 기존 관련 저장소
- `xmrig-dashboard` — TUI 기반 XMRig 마이너 모니터링 대시보드 (참고: TUI 위젯 디자인)
- `Kingdoms3Lite` — 삼국지 턴제 전략 (참고: 전략 게임 메카닉)
- `Idle_RPG` — 방치형 던전 RPG (참고: 로그라이크 구조)
- `convenience24` — 편의점 경영 시뮬레이션 (참고: 경영 자원 관리)

---

## PART 1: 시스템 모니터 위장 (1개)

---

### 1. 💼 htop 경영 시뮬레이터 — CPU 그래프로 회사 경영하는 게임

**기술:** Python, textual, rich, psutil  
**난이도:** 보통

**내용:**
- htop/top과 동일한 UI(막대 그래프, 트리뷰, 색상)로 회사 경영 (매출=CPU, 재고=메모리, 부채=스왑, 퇴사위기=좀비 프로세스)
- 키 바인딩이 htop과 동일 (F9=해고, F7/F8=승진/감봉, t=조직도, u=부서 필터)
- 깜빡이는 알림이 실제 업무 알림처럼 표시 ("오늘 마감이요!")

**포인트:**  
화면 캡처 한 장만 보면 IT 운영팀이 서버 모니터링하는 것처럼 보이지만 실제로는 회사 경영 게임을 플레이 중

---

## PART 2: 에디터/도구 위장 (1개)

---

### 2. 📜 vim 로그 추리 RPG — 옛날 로그 분석하는 척하는 미제 사건 수사

**기술:** Python, curses, vim 키바인딩 커스텀  
**난이도:** 높음

**내용:**
- vim으로 `app.log`/`access.log` 같은 가짜 로그 파일을 열어 `:set nu`, `G`로 스크롤하는 화면
- 정규식/grep 패턴으로 단서 찾기 (`/pattern`, `n/N`), 의심 라인 `dd`로 제거, `:w`로 사건 파일 저장해 클리어
- 케이스 클리어 시 `:q!`로 자백 후 도주(포기) 가능, 실제 신입사원이 "와 진짜 일하는 줄 알았어요" 반응 유도

**포인트:**  
진짜 vim/grep 문법이 곧 게임 메카닉 — 키 바인딩을 배울 필요 없이 도구 사용 능력 그 자체가 플레이 스킬

---

## PART 3: 모니터링 대시보드 위장 (1개)

---

### 3. 📊 Grafana/Prometheus 대시보드 RTS — 알람 대응하는 척하는 실시간 전략

**기술:** Python, textual, matplotlib(ASCII 백엔드), Rich Live  
**난이도:** 높음

**내용:**
- 흐트러진 그래프 + 빨간 알람 + deploy 상태판 UI 안에서 디펜스/타워디펜스 진행
- 각 패널 = 영토, 알람 = 침공, deploy = 유닛 생산, rollback = 시간 역행 후퇴
- 키보드 단축키로 패널 점프(`j/k`), 유닛 선택, `1~9`로 빌드 — 마우스 없이 풀 조작

**포인트:**  
Slack에 스크린샷 공유하는 순간 "아 이거 게임이었어?" 폭로 리스크가 가장 큰 정체성 혼란 컨셉

---

## PART 4: 디버거 위장 (1개)

---

### 4. 🐛 gdb 던전 크롤러 — 메모리 누수 추적하는 척하는 로그라이크

**기술:** Python, prompt_toolkit, gdb 출력 포맷 흉내  
**난이도:** 높음

**내용:**
- gdb/lldb 디버거 콘솔 (`(gdb)` 프롬프트, Breakpoint 출력, watchpoint 알림) 외형
- 변수=스탯, 스택 프레임=층수, `step`=한 칸 전진, `next`=방 무시, `break function`=세이브, `info locals`=인벤토리
- `Segmentation fault (core dumped)` 메시지가 진짜 게임 오버 연출, 옆 사람 보면 "메모리 누수 추적 중이요"로 거짓말 가능

**포인트:**  
Segfault로 죽는 게임이라 "작업 중 멈춤"이 자연스러운 변명 — 가장 완벽한 업무 위장

---

## PART 5: 컨테이너/클러스터 위장 (1개)

---

### 5. ☸️ kubectl 클러스터 RPG — 노드 스케일링하는 척하는 함대 지휘관

**기술:** Python, textual, kubernetes Python client(선택), JSON pretty print  
**난이도:** 높음

**내용:**
- `kubectl get pods -w` 무한 스크롤, JSON 출력, status 변화로 위장한 함대 지휘 게임
- Pod=함선, Service=항로, Namespace=영토, Node=기항지, ReplicaSet=함대 편성, CrashLoopBackOff=기근/전염병
- `kubectl scale`=병력 증강, `kubectl delete pod`=함선 격침, `kubectl rollout undo`=시간 역행(1회), 인벤토리가 진짜 k8s YAML

**포인트:**  
GitOps 환경에서 스크린샷을 보면 DevOps 툴 사용으로 오해 — 가장 전문적인 위장력

---

## 📊 요약

| # | 이름 | 위장 화면 | 직군 매칭 | 위장력 | 중독성 | 구현 난이도 |
|---|------|----------|----------|:------:|:------:|:----------:|
| 1 | htop 경영 시뮬레이터 | 시스템 모니터 | IT 운영 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | 보통 |
| 2 | vim 로그 추리 RPG | 텍스트 에디터 + 로그 | 개발자 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 높음 |
| 3 | Grafana 대시보드 RTS | 모니터링 대시보드 | SRE | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 높음 |
| 4 | gdb 던전 크롤러 | 디버거 콘솔 | C/Rust 개발자 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 높음 |
| 5 | kubectl 클러스터 RPG | k8s 대시보드 | DevOps | ⭐⭐⭐ | ⭐⭐⭐⭐ | 높음 |
| 6 | tail -f 전 세계 뉴스 RPG | API 로그 스트림 | 백엔드/SRE | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 높음 |
| 7 | Excel/CSV 데이터 청소 디펜스 | 스프레드시트 | 데이터/마케팅 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | 보통 |
| 8 | SSH 접속 로그 미로 탈출 | auth.log | 보안 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 높음 |
| 9 | crontab 편집기 농장 | crontab -e | DevOps/SRE | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 보통 |
| 10 | tcpdump 패킷 낚시 | tcpdump 출력 | 네트워크/보안 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | 보통 |
| 11 | Jira/Linear 보드 점령 전쟁 | 칸반 보드 | PM/개발자/디자이너 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 높음 |
| 12 | git log 역사로의 시간여행 | git log 그래프 | 개발자 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 보통 |

---

## 🏆 ICBM2 최종 추천 Top 7 (라운드 2 갱신)

12개 모두 구현 가치 있음. 첫 프로토타입 권장 순서:

1. **#11 Jira/Linear 보드 점령 전쟁** — PM/개발자 99% 매일 봄 → 위장 불필요, **Trello API**로 위장 완성도 ↑↑, **동료 참여**로 사회적 게임
2. **#1 htop 경영 시뮬레이터** — 위장력 최강, 구현 진입장벽 최저, TUI 위젯(textual) 그대로 활용
3. **#7 Excel/CSV 데이터 청소 디펜스** — **비개발자 포함** 광범위, "데이터 정제 = 타워 디펜스" 전이 직관적
4. **#9 crontab 농장** — **팜빌 메카닉**이 중독성 최강, **진짜 cron 스케줄링** 능력 동시 향상
5. **#4 gdb 던전 크롤러** — Segfault 게임 오버가 가장 자연스러운 변명, C/Rust 개발자라면 즉시 공감
6. **#10 tcpdump 패킷 낚시** — 가장 의심 면책, **낚시 게임** 장르 + 네트워크 분석 학습
7. **#8 SSH 로그 호러** — 메타 게임의 극치, "이상 접근 추적 중"으로 동료까지 게임에 끌어들임

---

## PART 6: 로그 스트림 위장 (1개)

---

### 6. 📡 tail -f 전 세계 뉴스 RPG — API 로그 모니터링하는 척하는 세계 정세 시뮬

**기술:** Python, textual, Rich Live, json logger  
**난이도:** 높음

**내용:**
- `tail -f /var/log/api.log` 무한 스크롤 (INFO/WARN/ERROR 색상, 타임스탬프, request_id) UI
- API 요청 국가 = 영토, 응답 시간 = 군사력, 429 = 봉쇄, 503 = 내란
- `Ctrl+C` = 휴전, `grep` = 정보 수집, `awk '{print $4}'` = 리소스 분석, `wc -l` = 턴 카운트

**포인트:**  
SRE/백엔드 직군의 일상 모니터링 화면과 동일, 만국 데이터로 세계 분쟁/경제 위기 실시간 시뮬레이션

---

## PART 7: 스프레드시트 위장 (1개)

---

### 7. 📊 Excel/CSV 데이터 청소 디펜스 — 데이터 정제 업무 중 타워 디펜스

**기술:** Python, textual, csv 모듈, vim/VS Code 스프레드시트 뷰 흉내  
**난이도:** 보통

**내용:**
- `vim data.csv` 화면, 행/열/NaN/null 셀/중복 행 표시
- 결측치 = 침공로, 중복값 = 적 유닛, 이상치 = 보스
- `dd` (행 삭제) = 적 처치, `s/NaN/0/g` = 방어막, `Ctrl+Z` = 시간 역행(1회), `:sort` = 방어선 재배치, `awk '!seen[$0]++'` = 광역 공격

**포인트:**  
데이터 분석가/마케터 직장인의 100% 일상 화면, 진짜 데이터 정제 능력도 함께 향상

---

## PART 8: 보안 로그 위장 (1개)

---

### 8. 🔐 SSH 접속 로그 미로 탈출 — 이상 접근 추적하는 척하는 1인 호러 미스터리

**기술:** Python, prompt_toolkit, auth.log 포맷 흉내, curses  
**난이도:** 높음

**내용:**
- `/var/log/auth.log` 화면 — "Failed password for root from..." / "Accepted publickey for admin from..." 출력
- 플레이어 = 해커, 시스템 = AI 경비원, 침투 흔적을 정당한 로그 사이에 섞어야 함
- 로그 라인 작성 = 행동, `tail -f` = 시점, `last` = 위치 확인, `who` = NPC 위치

**포인트:**  
보안/SRE 직군이 "누가 내 서버 접근 시도 중?" 하면서 진짜로 추적 시작 — 메타 게임 폭발

---

## PART 9: 스케줄러 위장 (1개)

---

### 9. ⏰ crontab 편집기 농장 — 배치 작업 스케줄링하는 척하는 팜빌 시뮬

**기술:** Python, textual, crontab 포맷 흉내, Rich  
**난이도:** 보통

**내용:**
- `crontab -e` 화면, `* * * * *` 필드/사용자 명령어/이메일 알림 설정 UI
- 작물 = 크론 작업, 수확 = 실행 완료, 비료 = 리소스 할당
- 시간 필드 = 성장 속도, `*` = 자동화, `MAILTO=` = 시장 가격, `&&` = 작물 연결, `||` = 보험(실패 대비)

**포인트:**  
DevOps/SRE 필수 화면 + 팜빌 중독성 최강, 진짜 cron 스케줄링 능력 동시 향상

---

## PART 10: 네트워크 캡처 위장 (1개)

---

### 10. 🐟 tcpdump 패킷 낚시 — 네트워크 트래픽 분석하는 척하는 낚시 게임

**기술:** Python, textual, scapy(선택), tcpdump 출력 포맷 흉내  
**난이도:** 보통

**내용:**
- `tcpdump -i en0 -nn` 화면 — IP/포트/SYN·ACK·FIN 플래그/페이로드 일부 출력
- 물고기 = 데이터 패킷, 찌 = 필터, 미끼 = tcpdump 옵션
- `port 80` = 민물고기, `host 8.8.8.8` = 특정 종, `-X` = 미끼 강화(페이로드), `greater 1000` = 대형, `Ctrl+C` = 줄 당김

**포인트:**  
가장 "그냥 보고 있으면 일하는 것 같은" 화면, 비개발 직군도 의심 면책, 진짜 네트워크 분석 능력 학습

---

## PART 11: 프로젝트 관리 위장 (1개)

---

### 11. 📋 Jira/Linear 보드 점령 전쟁 — 일정 관리하는 척하는 영토 분쟁 턴제 전략

**기술:** Python, textual, Trello/Jira REST API, JSON  
**난이도:** 높음

**내용:**
- 칸반 보드 (Backlog / In Progress / Review / Done) 화면, 티켓 카드/담당자/라벨/마감일 표시
- 컬럼 = 영토, 티켓 = 부대, 담당자 = 장수, 마감일 = 강화 데드라인
- 카드 이동 = 영토 진격, 라벨 색 = 진영, 담당자 변경 = 부대 재편성, 우선순위 = 전략 자원, 코멘트 = 외교(동맹/전쟁)

**포인트:**  
PM/개발자/디자이너 99% 매일 보는 화면, 동료가 "야 이거 옮겨도 돼?" 하며 같이 플레이 — 사회적 게임

---

## PART 12: VCS 위장 (1개)

---

### 12. 🕰️ git log 역사로의 시간여행 — PR 리뷰하는 척하는 인과 시뮬레이션

**기술:** Python, textual, git 명령어 wrap, Rich  
**난이도:** 보통

**내용:**
- `git log --graph --oneline --all` 화면 — commit 해시/브랜치 그래프/메시지/작성자/날짜
- 인과 시뮬레이션: "만약 이 commit을 revert했다면?" 을 진짜로 되돌려보며 평행 역사 체험
- `git checkout <hash>` = 시간 점프, `git revert` = 역사 변경, `git rebase` = 강제 병합(저주), `git reset --hard` = 기억 소거(HP 50% 감소), `git reflog` = 모든 시간축(치트키)

**포인트:**  
진짜 git 워크플로우가 게임 진행, `git push --force` 한 번이 세계 멸망을 부르는 긴장감, 실수로 develop 브랜치 망가뜨릴 위험 = 게임 내 패널티
